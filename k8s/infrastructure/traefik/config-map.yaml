apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-config
  namespace: traefik
data:
  traefik.yml: |
    # insecure exposes the read-only API/dashboard on :8080, which only the
    # LAN-side LB (192.168.1.117) reaches — used by nest-mcp health checks.
    api:
      dashboard: true
      insecure: true

    entryPoints:
      traefik:
        address: ":8080"
      web:
        address: ":80"
        http:
          redirections:
            entryPoint:
              to: websecure
              scheme: https
              permanent: true
      websecure:
        address: ":443"
        proxyProtocol:
          trustedIPs:
            - "10.10.0.1/32"
        forwardedHeaders:
          trustedIPs:
            - "127.0.0.1/32"
            - "192.168.0.0/16"
        http:
          middlewares:
            - traefik-rate-limit@kubernetescrd

    providers:
      kubernetesCRD:
        allowCrossNamespace: true
        allowExternalNameServices: true

    metrics:
      prometheus:
        addEntryPointsLabels: true
        addRoutersLabels: true
        addServicesLabels: true

    ping: {}

    log:
      level: INFO

    accessLog:
      format: json
