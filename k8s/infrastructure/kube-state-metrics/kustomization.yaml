apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml  # kept raw — custom pod-security label
  - helmrelease.yaml  # prometheus-community chart — replaces rbac+deployment+service
