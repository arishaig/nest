apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: medialyze
  namespace: media
spec:
  interval: 1h
  # Explicit so kustomize-controller owns this field: a stray
  # `flux suspend` is reverted on the next apps reconcile instead of
  # silently freezing the release (25 media HRs sat suspended
  # 2026-08-02..09-24). For break-glass, suspend the `apps` Kustomization.
  suspend: false
  chart:
    spec:
      chart: app-template
      version: 4.6.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      medialyze:
        strategy: Recreate
        containers:
          app:
            # ghcr.io/frederikemmer/medialyze:latest — rolling tag, no Renovate annotation.
            image:
              repository: ghcr.io/frederikemmer/medialyze
              tag: latest
            env:
              TZ: America/Los_Angeles
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            # 1Gi: 512Mi OOM-killed the scanner on large library passes.
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 1Gi
    service:
      app:
        controller: medialyze
        type: ClusterIP
        ports:
          http:
            port: 8080
            targetPort: 8080
    persistence:
      config:
        existingClaim: medialyze-config
        advancedMounts:
          medialyze:
            app:
              - path: /config
      media:
        existingClaim: media-nfs
        advancedMounts:
          medialyze:
            app:
              - path: /media
                readOnly: true
