apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subgen-models
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 5Gi
