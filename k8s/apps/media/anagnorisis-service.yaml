apiVersion: v1
kind: Service
metadata:
  name: anagnorisis
  namespace: media
spec:
  selector:
    app: anagnorisis
  ports:
    - name: http
      port: 5001
      targetPort: 5001
  type: ClusterIP
