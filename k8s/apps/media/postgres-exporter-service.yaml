apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: media
spec:
  type: NodePort
  selector:
    app: postgres-exporter
  ports:
    - port: 9187
      targetPort: 9187
      nodePort: 30187
