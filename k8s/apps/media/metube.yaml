apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metube
  namespace: media
spec:
  interval: 1h
  # Explicit so kustomize-controller owns this field: a stray
  # `flux suspend` is reverted on the next apps reconcile instead of
  # silently freezing the release (25 media HRs sat suspended
  # 2026-08-02..09-24). For break-glass, suspend the `apps` Kustomization.
  suspend: false
  chart:
    spec:
      chart: app-template
      version: 4.6.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      metube:
        strategy: Recreate
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
        containers:
          app:
            # ghcr.io/alexta69/metube:latest — rolling tag, no Renovate annotation.
            image:
              repository: ghcr.io/alexta69/metube
              tag: latest
            env:
              DOWNLOAD_DIR: /downloads
              TZ: America/Los_Angeles
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 1.5Gi
    service:
      app:
        controller: metube
        type: ClusterIP
        ports:
          http:
            port: 8081
            targetPort: 8081
    persistence:
      media:
        existingClaim: media-nfs
        advancedMounts:
          metube:
            app:
              - path: /downloads
                subPath: downloads/metube
