apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 5Gi
