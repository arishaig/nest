apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 50Gi
