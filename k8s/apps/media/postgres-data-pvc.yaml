apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 10Gi
