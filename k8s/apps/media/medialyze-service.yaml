apiVersion: v1
kind: Service
metadata:
  name: medialyze
  namespace: media
spec:
  type: NodePort
  selector:
    app: medialyze
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30807
