apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: copyparty-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 512Mi
