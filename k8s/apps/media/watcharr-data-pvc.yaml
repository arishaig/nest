apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: watcharr-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 1Gi
