apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: watchback-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-nvme
  resources:
    requests:
      storage: 512Mi
