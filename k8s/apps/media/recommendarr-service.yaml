apiVersion: v1
kind: Service
metadata:
  name: recommendarr
  namespace: media
spec:
  type: NodePort
  selector:
    app: recommendarr
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30809
