[database]
dbname = musicbrainz_db
host = ${MUSICBRAINZ_POSTGRES_SERVER}
password = ${POSTGRES_PASSWORD}
port = 5432
user = ${POSTGRES_USER}

[solr]
uri = http://${MUSICBRAINZ_SEARCH_SERVER}
batch_size = 200

[sir]
# import_threads was dropped to 4 during the 2026-08-01 incident: at 8
# threads under the *original* 1g indexer memory limit, the multiprocessing
# manager got OOM-killed mid-run, leaving the Solr index empty for months.
# Restored to upstream's default of 8 now that the indexer's limit is 4g
# (see memory-settings.yml.j2) — 4x the memory of the config that failed,
# traded for faster completion of the 2026-08-01 full reindex.
import_threads = 8
index_limit = 200000
live_index_batch_size = 100
max_retries = 4
poll_interval = 5
process_delay = 15
query_batch_size = 5000
wscompat = on

[sentry]
dsn =
